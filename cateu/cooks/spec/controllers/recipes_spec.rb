require 'rails_helper'

describe RecipesController do
  #some reason is calling .collect on nil?
  # describe '#create' do
  #   it 'creates a new recipe with the name given in params' do
  #     get :create
      # expect(Recipe.last.name).to eq "Bread"
    # end
  end

end
